[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notification-router"
version = "0.1.0"
description = "Architecture v0.1 deterministic router integration plumbing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
notification-router = "notification_router.main:main"
notification-router-smoke = "notification_router.smoke:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["notification_router*"]
